# Perfil activo por defecto (desarrollo)
spring.profiles.active=default

# Configuración de desarrollo
server.servlet.context-path=/vehiculos-api
server.port=8080

spring.data.mongodb.uri=mongodb+srv://davidluna333:crudv@cluster.ro6nou8.mongodb.net/vehiculosdb?retryWrites=true&w=majority&appName=Cluster

# Configuración MongoDB
spring.data.mongodb.auto-index-creation=true

# Logging para debug
logging.level.com.example.vehiculos=DEBUG
logging.level.org.springframework.web=DEBUG

# Recursos estáticos
spring.web.resources.static-locations=classpath:/static/
spring.mvc.static-path-pattern=/**